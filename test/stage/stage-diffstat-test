#!/bin/sh
#
# Test staging whole files and whole groups from the diffstat of the stage view.

. libtest.sh
. libgit.sh

export TEST_TIME_NOW=1401046937
export LINES=24

tigrc <<TIGRC
set diff-stat-group = yes
set vertical-split = no
TIGRC

steps '
	:enter
	:view-diff
	:save-display stage-diffstat.screen

	# "u" on a single stat entry stages just that file (search from the top so
	# the cursor lands on the stat line, not a diff header lower down).
	:0
	:/Button
	:status-update
	:save-display stage-after-file.screen

	# "u" on a group header stages every file of that group.
	:0
	:/store/
	:status-update
	:save-display stage-after-group.screen
'

git_init "$work_dir"

test_setup_work_dir()
{
	for path in \
		src/components/Button.tsx \
		src/components/Modal.tsx \
		src/store/actions.ts \
		src/store/reducers.ts \
		README.md
	do
		mkdir -p -- "$(dirname -- "$path")"
		printf 'a\nb\nc\n' > "$path"
		git add -- "$path"
	done
	git_commit --author="$IDENT_A" --message="Initial"
	for path in \
		src/components/Button.tsx \
		src/components/Modal.tsx \
		src/store/actions.ts \
		src/store/reducers.ts \
		README.md
	do
		printf 'a\nCHANGED\nc\n' > "$path"
	done
}

test_tig

assert_equals 'stage-diffstat.screen' <<EOF
 README.md   | 2 +-

[src/components/]
 Button.tsx  | 2 +-
 Modal.tsx   | 2 +-

[src/store/]
 actions.ts  | 2 +-
 reducers.ts | 2 +-

 5 files changed, 5 insertions(+), 5 deletions(-)

diff --git a/README.md b/README.md
index de98044..4657bb7 100644
--- a/README.md
+++ b/README.md
@@ -1,3 +1,3 @@
 a
-b
+CHANGED
 c
diff --git a/src/components/Button.tsx b/src/components/Button.tsx
[stage] Press '<Enter>' to jump to file diff - line 1 of 57                  38%
EOF

assert_equals 'stage-after-file.screen' <<EOF
 README.md                | 2 +-
 src/components/Modal.tsx | 2 +-

[src/store/]
 actions.ts               | 2 +-
 reducers.ts              | 2 +-

 4 files changed, 4 insertions(+), 4 deletions(-)

diff --git a/README.md b/README.md
index de98044..4657bb7 100644
--- a/README.md
+++ b/README.md
@@ -1,3 +1,3 @@
 a
-b
+CHANGED
 c
diff --git a/src/components/Modal.tsx b/src/components/Modal.tsx
index de98044..4657bb7 100644
--- a/src/components/Modal.tsx
+++ b/src/components/Modal.tsx
[stage] Unstaged changes - line 4 of 45                                      48%
EOF

assert_equals 'stage-after-group.screen' <<EOF
 README.md | 2 +-

[src/components/]
 Modal.tsx | 2 +-
 2 files changed, 2 insertions(+), 2 deletions(-)

diff --git a/README.md b/README.md
index de98044..4657bb7 100644
--- a/README.md
+++ b/README.md
@@ -1,3 +1,3 @@
 a
-b
+CHANGED
 c
diff --git a/src/components/Modal.tsx b/src/components/Modal.tsx
index de98044..4657bb7 100644
--- a/src/components/Modal.tsx
+++ b/src/components/Modal.tsx
@@ -1,3 +1,3 @@
 a
-b
[stage] Press '<Enter>' to jump to file diff - line 4 of 24                  91%
EOF
